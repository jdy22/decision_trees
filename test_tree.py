def predict_label(tree, test_instance):
    """ Predict label for test_instance using the tree.

    Args:
        tree (Node) : Root node of decision tree.
        test_instance (np.array) : 7-dimensional vector of attributes for one test instance.

    Outputs:
        int : Predicted label.
    """
    return None
